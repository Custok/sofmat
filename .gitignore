# --- Shoal: NUNCA publicar infra ni secretos ---
config.local.yaml
config.local.*
*.local.yaml
.env
.env.*
secrets/
# mapeos etiqueta->host reales, perfiles con datos de la flota
nodes.local.*
profiles/*.local.*
# leak-guard: denylist local con nuestros nombres reales (jamás al repo)
leak-guard/denylist.local.txt

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Modelos y pesos (nunca al repo)
*.gguf
*.safetensors
*.bin
models/

# Logs y medidas locales (pueden llevar hostnames/IPs)
*.log
logs/
bench_results/*.local.*

# Go build artifacts
/sofmat
/sofmat.exe
*.test
*.out

# Bundle llama embebido para el AppImage (908 MB) — JAMAS al repo
dist/llama-a3b1eff-sm120.tar.gz
dist/*.tar.gz

# build artifacts (release assets, never in repo)
*.exe
*.AppImage
.ver
.buildver
dist/soflink*
dist/node-agent*
dist/*.AppImage

# local ops / junk (never in repo)
/*.ps1
*.log
*.log.err
renames.local.json
.relver
dist/VERSION.txt
node-agent.exe
README.internal.md
