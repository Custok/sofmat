#!/usr/bin/env bash
# sofmat leak-guard pre-commit hook.
# Blocks the commit if staged content contains private infra or secrets.
# Install with: bash scripts/install-hooks.sh
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "${repo_root}" && go run ./cmd/leak-guard --staged
